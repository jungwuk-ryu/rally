# Rally 작업 규칙

- 기능 경계가 다른 작업은 병렬로 진행하되, 공통 타입·상태 계약 변경은 먼저 공유한다.
- 다른 작업자의 변경을 되돌리지 않는다. 충돌 시 현재 파일을 기준으로 통합한다.
- 기능이 완성되면 해당 경로만 검증하고 Conventional Commit으로 남긴다.
- UI 카피는 짧고 자연스러운 한국어로 쓴다. 투자 조언 표현은 쓰지 않는다.
- 실서비스 비밀값은 출력하거나 커밋하지 않는다. Gemini는 선택 기능이며 반드시 fallback을 둔다.
- 외부 API가 실패해도 호스트·손님·실시간 데모는 계속 동작해야 한다.
