# Rally 디자인 명세

## 기준 이미지

- `design/host-concept-v2.png`: 호스트 대형 화면 최종 방향
- `design/guest-concept.png`: 손님 모바일 화면

## 공통 시스템

- 브랜드: 짧은 필기체 Rally 워드마크, 큰 숫자와 또렷한 한글 중심
- 강조색: `#725CFF` 보라, `#318CFF` 블루, 상승 `#23D98B`, 하락 `#FF6B7A`
- 모션: 가격 변화는 배경의 색 밀도와 차트 선의 움직임으로 전달한다. `prefers-reduced-motion`에서는 정적인 색 변화로 줄인다.
- 아이콘: Lucide의 얇은 라운드 아웃라인을 쓴다. 아이콘만으로 의미가 불명확한 조작에는 짧은 한국어 라벨을 붙인다.

## 호스트 화면

- 배경: 검은 화면의 왼쪽에서 오른쪽 보라·모브·장밋빛으로 부드럽게 번진다. 카드가 화면을 가득 채우지 않는다.
- 중심: 크롬 질감의 큰 Rally 워드마크가 주인공이다. 종목·가격·차트는 워드마크를 해치지 않는 조용한 정보 레이어다.
- 오른쪽: 테두리만 은은한 좁은 반투명 순위 레일을 화면 끝에 붙인다.
- 좌하단: QR과 방 코드. 우상단: 설정 아이콘 하나. 하단은 콘서트 자막 같은 AI MC 한 줄이다.
- Rally Moment: 새 대시보드나 과한 버튼 대신 화면 전체의 빛이 잠깐 터지는 연출로 만든다.

## 손님 화면

- 배경: 진짜 흰색. 상단과 시세 카드만 보라-블루 그라데이션을 쓴다.
- 상단: 방 코드, 크레딧을 한 줄에 고정한다.
- 시세: 종목·가격·변동률·작은 차트를 하나의 큰 카드에 담는다.
- 투자: 금액, 진입가, 예상 정산을 한눈에 보이는 카드로 분리한다.
- 상품: 사진이 있는 가로 카드와 선물 보내기 동작을 쓴다. 주문/선물 흐름의 주인공은 사람과 상품이다.
- 하단: 투자·상품·순위 세 탭. 중요한 알림은 하단 toast로 짧게 보여준다.

## 구현 전 카피 목록

호스트 상단: `Rally`, `파티 참여하기`, `방 코드`, `RALLY`

손님 상단과 투자: `크레딧`, `내 투자`, `진입가`, `예상 정산`, `투자하기`

공통 알림: `라운드가 시작됐어요`, `Rally Moment!`, `하이볼을 쐈어요`, `라운드 정산 완료`

## 의도적 차이

기준 이미지는 완성된 정지 상태다. 구현 화면에는 사용자가 요청한 설정, 이벤트, 주문, 충전, 가입, 정산, 실시간 상태를 실제 인터랙션으로 추가한다. 이 추가 영역도 같은 색·타입·유리 패널 규칙으로 만든다.
